name: CI
on:
  push:
    branches: [master, dev]
  pull_request:
jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run check
        run: ./check.sh
